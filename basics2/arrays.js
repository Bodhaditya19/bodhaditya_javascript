var numbers = [1, 2, 3];
for(var i = 0; i<numbers.length; i++){
    console.log(numbers[i]);
}
console.log(numbers[numbers.length-1]);
console.log('the length ${2+1} of our array is');
//console.log(numbers.length);